export const APP_PORT = 3000;
export const PROXY_PORT = APP_PORT + 1;

const API_PORT = 1144;
const API_PORT_GO = 5050;

export const APP_BASE_URL = `http://localhost:${APP_PORT}`;
export const API_BASE_URL_KT = `http://localhost:${API_PORT}/api/kt`;
export const API_BASE_URL_GO = `http://localhost:${API_PORT_GO}/api/go`
export const PROXY_BASE_URL = `http://localhost:${PROXY_PORT}`;
